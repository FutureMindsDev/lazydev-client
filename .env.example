# Backend API base URL. Default points at a local LazyDev backend.
NEXT_PUBLIC_API_URL=http://localhost:3200

# Enable MSW mock API (default: enabled). Set to 'false' to hit the real backend.
NEXT_PUBLIC_ENABLE_MOCKS=true
